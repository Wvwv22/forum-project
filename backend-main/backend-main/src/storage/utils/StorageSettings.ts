import { IUploadSettings } from '../intefaces/UploadSettings.Interface';

export const AVATAR_SETTINGS = {
  bucket: 'avatars',
  filename: '',
  max: 1,
  width: 200,
  height: 200
} as IUploadSettings;
